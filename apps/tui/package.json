{
  "name": "@repo/tui",
  "version": "0.2.0-alpha",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/main.tsx",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @repo/tui'",
    "test": "echo 'No tests configured for @repo/tui'"
  },
  "dependencies": {
    "@repo/agent-core": "*",
    "ink": "^6.6.0",
    "marked": "^15.0.12",
    "marked-terminal": "^7.3.0",
    "react": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
