{
  "name": "open-codex-ui",
  "version": "0.1.0-alpha.1",
  "description": "Codex-style desktop and terminal interfaces with shared agent core in a Turborepo monorepo.",
  "private": true,
  "packageManager": "npm@11.7.0",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run @repo/api#dev @repo/web#dev @repo/electron#dev --ui=tui",
    "start": "turbo run @repo/api#dev @repo/web#dev @repo/electron#dev --ui=tui",
    "dev:web": "turbo run @repo/web#dev --ui=tui",
    "dev:desktop": "turbo run @repo/api#dev @repo/web#dev @repo/electron#dev --ui=tui",
    "dev:tui": "turbo run dev --filter=@repo/tui",
    "dev:api": "turbo run dev --filter=@repo/api",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "release:check": "node ./scripts/release/check-release-ready.mjs",
    "release:notes": "node ./scripts/release/extract-release-notes.mjs v$npm_package_version"
  },
  "devDependencies": {
    "turbo": "^2.5.5",
    "typescript": "^5.3.3"
  }
}
